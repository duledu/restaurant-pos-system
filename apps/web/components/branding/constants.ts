/**
 * Naziv PROIZVODA (TableCore POS platforma) — odvojeno od bilo kog naziva
 * restorana koji je koristi. Ovo je plain modul (bez "use client") da bi
 * bio bezbedan za import i iz Server Component-a (npr. metadata u
 * app/layout.tsx), ne samo iz AppLogo.
 */
export const APP_NAME = "TableCore";
