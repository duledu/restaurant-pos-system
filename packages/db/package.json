{
  "name": "@rcs/db",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "ts-node prisma/seed.ts",
    "seed:menu": "ts-node prisma/seed-menu.ts",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
